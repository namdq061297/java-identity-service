server:
  port: 8080
  servlet:
    context-path: /identity
    include-message: always
spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/identity_service"
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
jwt:
  signerKey: "temporary-signing-key-for-dev-only-2026-please-change"

